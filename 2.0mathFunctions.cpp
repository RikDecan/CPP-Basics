#include <iostream>
#include <cmath>

int main() {

    double x = 3.99;
    double y = 4;
    double z;

    // z = std::max(x, y); // grootste getal tussen x & y => 4
    // z = std::min(x, y); // kleinste getal tussen x & y => 3
    // z = pow(2, 4);  //twee tot de vierde macht => 16
    // z = sqrt(9); //vierkantswortel => 3
    // z = abs(-3); // absolute waarde => 3 
    // z = round(x); //afronden boven of beneden
    // z = ceil(x); //afronden naar boven 
    // z = floor(x); //afronden naar beneden

    std::cout << z << std::endl;
    return 0;
}


//https://cplusplus.com/reference/cmath/